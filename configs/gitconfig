[user]
    name = PhongsakornP.
    email = phongsakornp@gmail.com
[color]
    ui = true
[core]
    editor = vim
    pager = less
[alias]
    lg = log --topo-order --all --graph --date=local --pretty=format:'%C(green)%h%C(reset) %><(55,trunc)%s%C(red)%d%C(reset) %C(blue)[%an]%C(reset) %C(yellow)%ad%C(reset)%n'
    sw = show --pretty='format:' --name-only
    st = status
[diff]
    tool = vimdiff
[merge]
    tool = vimdiff
[difftool]
    prompt = false
[rebase]
    autostash = true
[pull]
    rebase = true